<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container">
		<a class="navbar-brand" href="/"><img src="/img/vintko_logo_white.png" alt="Vintko logo"
				class="navbar-brand-image"></a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
			aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
			<div class="navbar-nav ms-auto">
				<a class="nav-item nav-link" href="/"><i class="fa-solid fa-clock"></i> Watches</a>
				{{#if user}}
				{{#if user.admin}}
				<a class="nav-item nav-link" href="/messages"><i class="fa-solid fa-message"></i> Messages</a>
				<a class="nav-item nav-link" href="/users"><i class="fa-solid fa-user-group"></i> Users</a>
				{{/if}}
				<a class="nav-item nav-link" href="/signout"><i class="fa-solid fa-right-from-bracket"></i> Sign out</a>
				{{else}}
				<a class="nav-item nav-link" href="/signin"><i class="fa-solid fa-right-to-bracket"></i> Sign In</a>
				{{/if}}
			</div>
		</div>
	</div>
</nav>
<div class="container text-end">
	{{#if user}}
	<small>Hey, {{user.email}}!</small>
	{{else}}
	<small>Anyone there?</small>
	{{/if}}
</div>
<h2 class="text-center">{{title}}</h2>
{{#if msg}}
<div class="container">
	<p class="message">{{msg}}</p>
</div>
{{/if}}